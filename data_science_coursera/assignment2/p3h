select sum(a.count*b.count) from frequency as a, frequency as b
where a.term = b.term
AND a.docid='10080_txt_crude'
AND b.docid='17035_txt_earn'
;

--select sum(a.value*b.value) 
--from frequency as a, frequency as b
--where a.col_num = b.row_num
--AND a.row_num = 2
--AND b.col_num = 3;
